name: sys_agents
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pip
  - python-dotenv
  - pip:
    - pytest>=9.0.0
    - pytest-asyncio>=0.25.0
    - pytest-cov>=5.0.0
    - ruff>=0.6.0
    - black>=24.0.0
    - isort>=5.13.0
